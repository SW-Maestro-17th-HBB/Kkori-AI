# Kkori Worker 설계 결정 노트

> 워커 설계 과정에서 내린 결정을 **근거·트레이드오프와 함께** 남기는 학습 노트.
> 백엔드 `docs/drafts/resume-upload-walkthrough.md`의 방식을 미러링한다 — 각 절은
> "무엇을 골랐나 + **왜** + 버린 대안"으로 쓰고, 끝의 **💬 면접 포인트**는 이 결정에서 나올 질문과 답의 핵심.
> 확정 사실의 정본은 PRD(`worker/docs/requirements/resume-analysis/pipeline.md`), 여기는 **왜**를 담는다.
> 새 결정이 생기면 절을 이어붙인다(append-only, 번복 시 해당 절에 취소선·사유 추가).

---

## 1. 워커는 별도 레포 — 백엔드 안에 두지 않는다

> **일부 번복 (2026-07-16)**: "백엔드와 분리"는 유지되나, 완전 독립 레포가 아니라 **기존 `Kkori-AI` 모노레포의
> `worker/` 하위**로 확정됨(팀이 이미 agent/worker/shared 구조로 세팅). 아래 근거 중 "백엔드와 안 섞는다"는
> 그대로 유효하고, "독립 레포" 부분만 바뀜. AI 컴포넌트끼리는 임베딩 계약을 `shared/`로 공유해야 해서
> 모노레포가 오히려 맞았다.

**결정**: 워커는 `Kkori-Worker`(독립 git repo). 백엔드 `Kkori-Backend` 안의 서브디렉터리가 아니다.

**왜**: 언어·런타임·배포 주기·의존성이 완전히 다르다(Python/uv/FastStream vs Java/Gradle/Spring). 한 레포에
섞으면 CI·의존성 그래프·리뷰 도구가 두 세계를 억지로 공유한다. "백엔드와 연동이 쉬울 것"이라는 이유로
같이 두려 했지만, 연동의 실체는 **코드 공유가 아니라 계약 공유**(Redis 스트림 필드·상태 enum)라 레포가
같을 필요가 없다. 계약은 문자열 메시지로 언어 경계를 넘는다.

**대가**: 계약이 두 레포에 물리적으로 나뉘어 산다 → §4(자기완결 사본)로 관리.

💬 **면접 포인트**: "폴리글랏 서비스를 모노레포 vs 멀티레포 어디에?" — 결합의 축이 코드인지 계약인지로 판단.
언어 경계를 넘는 결합은 메시지 계약이라 레포 분리가 자연스럽다.

---

## 2. 메시지 처리 프레임워크: FastStream (vs Celery)

**결정**: FastStream 0.7.1 (Redis 브로커). Celery 아님.

**왜**:

| 축 | FastStream | Celery |
|---|---|---|
| 모델 | **브로커 네이티브**(스트림 구독/발행이 1급) | 태스크 큐(작업 디스패치 중심) |
| async | **native async/await** (AI·S3 I/O 대기에 적합) | 주로 동기 워커(prefork), async는 곁다리 |
| Redis Streams | 소비자 그룹·ACK를 그대로 노출 | Streams를 1급으로 다루지 않음 |
| 계약 | pydantic 모델로 메시지 스키마 = 코드 | 태스크 시그니처(인자)가 계약 |
| 무게 | 얇음, 브로커 스위칭 용이 | 무겁고 기능 많음(스케줄러 등) |

우리 워크로드는 "이력서 1건 = 여러 외부 I/O(S3 다운로드·Bedrock 2종 호출·DB)를 대기하는 async 파이프라인"이라
native async + Redis Streams 소비자 그룹을 1급으로 다루는 FastStream이 결이 맞는다. Celery의 강점(주기 태스크
스케줄링, 방대한 생태계)은 지금 필요 없다.

**대가**: Celery보다 생태계·레퍼런스가 적다(신생). 운영 노하우가 덜 쌓임.

💬 **면접 포인트**: "Celery 안 쓴 이유" — 태스크 큐 vs 브로커 네이티브. I/O 바운드 async 파이프라인 + Streams
소비자 그룹이 필요하면 후자. 프레임워크는 워크로드 모양으로 고른다.

---

## 3. Redis Streams (vs Pub/Sub / List)

**결정**: 요청 채널은 **Redis Streams + Consumer Group + XACK**. (백엔드가 이미 Stream으로 발행.)

**왜 — 전달 보장이 채널마다 다르다**:

| 방식 | 보존 | 재소비 | 우리 용도 적합성 |
|---|---|---|---|
| Pub/Sub | 없음(구독자 없으면 소실) | 불가 | ✗ 워커 다운 중 요청 증발 |
| List(BLPOP) | 있음(FIFO) | 소비 시 제거 → PEL 없음 | △ ACK·재처리 없음 |
| **Streams+Group** | 있음(로그) | **PEL·XACK·XAUTOCLAIM** | ◎ at-least-once 재처리 |

이력서 분석은 **유실되면 안 되는 작업**이다. 워커가 처리 중 죽으면 메시지가 PEL(Pending)에 남아
XAUTOCLAIM으로 회수·재개할 수 있어야 한다. 이건 Streams 소비자 그룹만 제공.

- 반대로 **상태 채널(status.changed)은 유실 허용** → 백엔드는 SSE 중계에 auto-ack(부기 없음)로 소비.
  "같은 Redis, 다른 채널, 다른 보장" — 전달 보장 요구가 기술 선택을 가른다.

**딸린 결과**: at-least-once → **멱등성 필수**(§5), 중복·회수 경합 방어(원자적 상태 CAS) 필요.

💬 **면접 포인트**: "Pub/Sub vs Stream", "at-least-once와 멱등성", "PEL/XACK/XAUTOCLAIM로 크래시 복구",
"한 시스템 안에서 채널마다 보장을 다르게 두는 이유".

---

## 4. 계약은 워커 레포에 자기완결 사본으로 둔다 (변경 권한은 백엔드)

**결정**: 워커 PRD가 계약(스트림 필드·상태 8종·StructuredData)의 **전체 사본**을 담는다. 단, **바꿀 권한은
백엔드**에 있고 워커는 소비자로서 따른다.

**왜 사본인가**: 크로스 레포라 **워커의 CodeRabbit·CI는 백엔드 레포를 볼 수 없다.** "백엔드 문서를 참조"는
워커 리뷰 시점에 물리적으로 열리지 않으므로 무의미 → 계약을 워커 안에 물리적으로 복제해야 리뷰·개발이 성립.

**왜 권한은 백엔드인가**: 계약의 실제 소유자는 **발행자이자 상태 엔티티의 주인**인 백엔드다. 사본을 가졌다고
워커가 계약을 바꿀 수 있는 건 아니다 — "물리적 위치(사본)"와 "변경 권한(백엔드)"을 분리.

**계약 불일치 방어 — 계약 예시 대조 테스트**: 사본은 알아채지 못한 채 어긋날 수 있다. 실제 스트림 필드맵 예시(JSON)를 워커에
계약 예시 파일로 커밋하고, **요청 decode(문자열맵→pydantic) + 상태 encode(모델→문자열맵) 양방향**을 그 예시와
대조하는 테스트를 둔다. 계약이 바뀌면 예시 파일도 바뀌고 그 변경이 PR diff에 **그대로 드러난다**. (백엔드의 "계약 record의
`toMap()`을 테스트가 그대로 쓴다"와 같은 방식 — 계약을 컴파일/테스트가 붙잡게 한다.)

**대가**: 두 곳을 수동 동기화. 백엔드 record 변경 시 워커 계약 예시·모델을 손으로 맞춰야 함(자동 전파 없음).

💬 **면접 포인트**: "언어·레포 경계를 넘는 계약을 어떻게 관리하나" — SSOT의 두 얼굴(변경 권한 vs 물리 사본)을
분리, 크로스 레포 리뷰 도구의 가시성 한계, 계약 예시 대조로 불일치를 테스트로 막기.

---

## 5. 멱등성 & 크래시 복구 = DB 상태 기준 체크포인트

**결정**: 진입 지점은 **DB 상태가 결정**(mode는 cross-check). 각 단계 진입을 **원자적 상태 CAS**로,
산출물 저장과 상태 전이를 **한 트랜잭션**으로.

**왜**: at-least-once(§3) + XAUTOCLAIM 회수는 "안 죽은 원본 + 회수본"이 경합할 수 있다. "resumeId 멱등"만으론
이중 처리를 못 막는다. `UPDATE ... SET status='PARSING' WHERE status='UPLOADED'`처럼 CAS로 전이하고 **영향
행이 0이면 조용히 양보** → 앞서간 처리자가 이미 있다는 뜻.

**두 재개 불변식**(둘 다 "상태가 진실")：
1. **산출물 커밋 후 상태 갱신** — data 저장 뒤에 PARSED. 반대면 "PARSED인데 data 없음"으로 재개가 깨짐.
2. **재개 분기는 상태 컬럼만 본다, 산출물 존재로 판단 금지** — FULL 재분석은 상태를 UPLOADED로 리셋하지만
   이전 런의 `structured_data`는 **DB에 남아 있다**. 산출물 보고 "임베딩부터" 판단하면 **낡은 데이터로 색인**.

**임베딩 진입 시 항상 기존 청크 선삭제** 후 재생성(EMBEDDING 재개·REINDEX·부분 실패 공통) → 중복/유령 청크 방지.

💬 **면접 포인트**: "at-least-once에서 정확히 한 번 효과내기"(멱등·CAS), "체크포인트를 산출물이 아니라 상태로
잡는 이유", "동시 처리 경합을 락 없이 막기"(조건부 UPDATE의 영향 행 수).

---

## 6. 포기 규칙 & DLQ 미도입 & retry_count 소유권

**결정**:
- **포기**: 수신·회수 직후 처리 전에 delivery count 확인, 임계(3) 이상이면 재처리 없이 **① `FAILED` 기록 →
  ② XACK** (이 순서 고정, ①은 멱등).
- **DLQ 스트림 안 만든다**: 메시지의 모든 필드가 **DB에서 재유도 가능한 포인터**라 격리 보존할 고유 정보가 없다.
  재처리는 스트림 재주입이 아니라 **재분석 API가 DB에서 새 메시지를 생성**. 격리 건은 `FAILED` 레코드가 겸함.
- **`retry_count`는 Worker 소유**(서버 read-only). **delivery count(Redis PEL)와 다른 카운터** — 전자는
  "런 내부 재시도"(Worker DB 기록), 후자는 "메시지 전달 횟수"(Redis 관리). 서로 파생 안 됨.

**왜 순서가 FAILED→XACK인가**: 중간에 죽어도 재전달본이 같은 경로를 다시 타 수렴(①이 멱등이라 이미 FAILED면
그대로). 반대로 XACK 먼저면 FAILED 기록 전에 죽을 때 "종결도 실패도 아닌" 미아가 생김.

**왜 DLQ가 과한가**: DLQ의 가치는 "재처리에 필요한데 원본에만 있는 정보를 격리 보존"인데, 여기 메시지는
포인터(resumeId 등)뿐이라 보존할 게 없다. `FAILED`가 같은 일을 더 단순하게 한다(같은 테이블에서 조회·구분).

💬 **면접 포인트**: "실패 메시지 어디로 보내나" — DLQ가 항상 답은 아니다, 메시지가 포인터면 상태 레코드가 대체.
"쓰기 두 개의 순서"(FAILED→ACK)로 크래시 창을 닫기, "두 재시도 카운터의 구분".

---

## 7. AI 스택: Claude Haiku 4.5 (구조화) + Cohere Embed Multilingual v3 (임베딩)

> **일부 갱신 (2026-07-21)**: 임베딩은 **Embed v4(`cohere.embed-v4:0`)로 변경** — 서울 리전(확정)에 v3 미제공,
> v4 가 상위 호환(다국어 성능 향상)이며 **출력 차원을 1024 로 지정**할 수 있어 `vector(1024)` 스키마·비대칭
> 임베딩(search_document/search_query) 모두 유지됨. 아래 "왜 Cohere 계열인가" 근거는 그대로 유효.
> Claude 는 서울에서 cross-region 프로파일(`global.` 접두사)로만 제공 — 추론이 전 세계로 라우팅될 수 있음을
> 인지하고 수용(호출 엔드포인트는 서울).

**결정**: 구조화 LLM = **Claude Haiku 4.5(Bedrock)**, 임베딩 = **Cohere Embed Multilingual v3, `vector(1024)`**.

**구조화 LLM — 왜 Haiku**:
- 작업이 "PDF 텍스트 → 고정 스키마(§StructuredData) JSON"이라 **추론 난도보다 스키마 준수**가 핵심 →
  tool-use/JSON 스키마 강제가 되는 모델이면 충분. Haiku가 그걸 저렴하게(Sonnet 대비 **정확히 1/3**: $1/$5 vs
  $3/$15 per 1M in/out) 한다.
- **승급 경로가 무리스크**: 한국어 파싱 품질이 부족하면 Sonnet 4.6으로 교체 — 스키마·DB 영향 0(출력 형태 동일).
  그래서 싼 쪽에서 시작해도 손해가 없다.
- 비-Claude 대안(Nova 등)도 있으나 tool-use JSON 신뢰도·품질 마진에서 Claude 우선.

**임베딩 — 왜 Cohere v3 (vs Titan V2)**:

| | Cohere Embed ML v3 | Titan Embed V2 |
|---|---|---|
| 차원 | 1024 | 1024 |
| 한국어/다국어 | **강함** | 약함 |
| 단가 | 비쌈(~5배) | 쌈 |
| 비대칭 임베딩 | `search_document`/`search_query` | 없음 |

이력서 RAG의 본질은 **한국어 검색 품질**이라 다국어 약한 Titan은 탈락. 비용차(~5배)는 이력서 볼륨에선 무시
가능(1만 건 ≈ $3). 색인은 `input_type=search_document`, 질의는 `search_query`로 **비대칭 임베딩** — 문서/질의를
다른 공간 취급해 검색 정확도를 올린다.

**스키마 소유권**: `resume_chunks`(content, metadata, `embedding vector(1024)`) + `CREATE EXTENSION vector`는
**Worker 소유**(유일 writer이자 임베딩 차원의 주인). 차원 1024는 **Cohere v3에 종속** — 모델 교체 시 여기만 조정.
백엔드는 `resumes`/`structured_data` 소유.

**운영 주의**: Bedrock은 **로컬 에뮬레이터가 없다**(MinIO=S3와 다름). 로컬 워커도 실제 Bedrock 호출 → AWS 자격·
리전 필요, 로컬에서도 과금. 서울 리전 모델 가용성 제한 가능 → **us-east-1 폴백**을 config에.

💬 **면접 포인트**: "LLM/임베딩 모델 선택 기준" — 작업이 추론이냐 형식준수냐(→ 싼 모델+스키마 강제), 검색은
도메인 언어 품질 우선(비용은 볼륨으로 환산). "비대칭 임베딩", "임베딩 차원이 스키마를 잠근다(교체 비용)".

---

## 8. REINDEX 진입 상태: Spring이 발행과 같은 트랜잭션에서 세팅

**결정**: 런의 초기 상태를 **Spring이 발행과 같은 트랜잭션에서** 세팅한다 — FULL→`UPLOADED`,
REINDEX→`EMBEDDING`(백엔드 `restartFor()`).

**왜 — 설계 모순을 없앤다**: 원래 "Spring은 UPLOADED만 기록 + 워커는 순수 상태 기준"이었는데, REINDEX는
구조화를 건너뛰고 임베딩부터여야 한다. 만약 REINDEX도 UPLOADED로 진입하면 워커의 상태 기준 재개가 처음부터
돌려 **LLM이 사용자 수정본을 덮어쓴다**. Spring이 진입 상태를 EMBEDDING으로 세팅하면 워커는 상태 기준만 유지해도
자연히 임베딩부터 탄다. 부수 효과로 **REINDEX 중복 전달도 멱등**(완료 시 EMBEDDED→이후 스킵).

- 원칙 정리: "Spring은 UPLOADED만" → "Spring은 **런의 초기 상태**를 기록". 이후 파이프라인 전이는 여전히 전부 워커.
- **cross-check로 시끄럽게**: mode와 상태가 규약과 어긋나면(REINDEX인데 UPLOADED 등) 경고 후 진행이 아니라
  **`FAILED`로 종결** — 조용한 오염보다 시끄러운 실패.

💬 **면접 포인트**: "재색인/재처리에서 어디서부터 다시 하나" — 진입 상태를 발행자가 트랜잭션으로 세팅해 소비자를
단순 상태 기계로 유지. "계약 위반을 조용히 넘기지 않기(loud failure)".

---

## 9. 청킹: 엔티티 기반 + 오버플로 분할 (C)

**결정**: `structured_data`의 **엔티티 경계를 청크 경계로** — 프로젝트/경력/스킬 카테고리당 1청크, 초장문
엔티티만 문장 경계로 분할. (PRD §2.5)

**왜 — 입력이 이미 구조화돼 있다**: 청킹 입력은 raw text가 **아니라** profile/skills/projects/experiences로
구조화된 JSON(원문 미저장). 고정 길이 슬라이딩 윈도우는 raw text에서 구조를 **찾으려는** 기법인데, 우리는 구조를
**이미 갖고 있다** → 그걸 버리고 잘라 섞을 이유가 없다.

**대안 비교**:

| 전략 | 검색 정확도 | 자기완결성 | 문제 | 판정 |
|---|---|---|---|---|
| A. 고정 길이 슬라이딩 | △ 경계서 잘림·혼합 | △ | 있는 구조를 버림 | ✗ |
| **C. 엔티티 기반 + 오버플로 분할** | ◎ 의미경계=검색단위 | ◎ | 초장문만 분할 필요 | **채택** |
| D. 전체 1청크 | ✗ 어느 프로젝트인지 못 집음 | ○ | 세밀검색 불가 | ✗ |
| E. 필드 초세분화 | △ 과분할 | ✗ "Java"만 든 청크 무의미 | 문맥 파편화·청크 폭증 | ✗ |
| F. 계층형 parent-child | ◎ | ◎ | 부모-자식 저장 복잡 | △ 미래 |

**핵심 규칙**: profile은 임베딩 제외(질문 소스 아님), 자기완결 라벨 prepend(`[프로젝트] {name} · 역할 {role}...`),
초장문만 문장 분할+헤더 반복+소량 오버랩, 빈 것은 청크 0개(→ 0청크 EMBEDDED 종결).

**왜 지금 확정해도 안전한가 — 되돌릴 수 있다**: 청킹은 순수 워커 내부 로직이라 **계약·임베딩 차원 불변**.
바꾸면 코드 수정 + **REINDEX 한 바퀴로 백필**(DDL 마이그레이션 없음). 임베딩 모델 교체(차원 바뀌면 스키마까지)
보다 싸다. 백필 대상 식별용으로 `metadata.chunk_version`을 심어둔다. → 첫 버전을 과튜닝하지 않고, 검색 품질을
실측하며 파라미터나 F(계층형)로 승급.

💬 **면접 포인트**: "청킹 전략을 무엇으로 정하나" — **입력이 raw text냐 구조화 데이터냐**가 가른다. 구조가 있으면
그 경계가 곧 검색 단위. "청킹은 되돌릴 수 있는 결정"(REINDEX 백필, 차원 불변)이라 초기에 과설계 불필요.

---

## 10. 청크 크기 측정: 근사 토큰(글자수÷2) + 정규식 문장 분할

**결정**: 청킹의 "512토큰 초과" 판정은 **정확한 토크나이저 없이 근사**한다 — `approx_tokens(text) = len(text) // 2`
(한국어 대략 2글자 ≈ 1토큰). 문장 분할도 NLP 라이브러리 없이 **정규식**(`.` `!` `?` 줄바꿈)으로 한다. (`chunking.py`)

**배경 — 토큰이란**: AI 모델은 텍스트를 글자가 아니라 자기 사전 기준의 조각(토큰)으로 읽는다. 몇 토큰인지 정확히
알려면 **그 모델(Cohere)의 토크나이저를 돌려야** 한다. 우리의 512는 Cohere가 잘 처리하는 입력 크기에서 온 목표치.

**왜 근사로 충분한가**:
1. **512는 딱딱한 한도가 아니라 소프트 목표** — 청크가 400이든 600이든 검색 품질이 급락하지 않는다. ±30% 오차 허용.
2. **이력서 항목은 대부분 짧다** — 수십~수백 글자라 애초에 분할 경로를 타지 않는다. 근사가 영향 주는 건 아주 긴
   항목뿐.
3. **정확한 셈의 비용** — Cohere 토크나이저 의존성 추가 + 특정 모델에 코드가 묶임. MVP에서 얻는 것 대비 비용이 크다.

**대안 비교**:

| 방법 | 정확도 | 의존성 | 판정 |
|---|---|---|---|
| **글자수÷2 근사** | 낮음(±30%) | 0 | **채택 (MVP)** |
| Cohere 토크나이저 | 정확 | 라이브러리 + 모델 종속 | 품질 튜닝 때 |
| tiktoken 등 타모델 토크나이저 | 중간(모델 다름) | 라이브러리 | 어중간해서 제외 |

**한계 (알고 쓰는 것)**:
- 영어·코드가 섞이면 **과대평가** — "Spring Boot"(11글자)는 실제 2~3토큰인데 근사는 5로 센다.
- 경계 근처(±30%)에서 **판정이 흔들림** — 안 잘라도 될 걸 자르거나 그 반대. 소프트 목표라 감수.
- **모델 입력 한도 보장이 아님** — 한도에 딱 맞춰야 하는 용도로는 쓰면 안 된다.
- 정규식 문장 분할도 근사 — 한국어 종결어미("~했음", "~함")나 소수점("3.5")에서 오분할 가능. 청크 경계가 한 문장
  어긋나는 정도라 허용.

**전환 조건**: 검색 품질 실측에서 청크 크기 관련 문제가 보이면 그때 Cohere 토크나이저(정확한 셈)·NLP 문장 분리(kss 등)로
올린다. 청킹은 REINDEX 백필로 되돌릴 수 있으므로(§9) 이 전환도 싸다. → PRD §10 열린 결정.

💬 **면접 포인트**: "토큰 수를 어떻게 세나" — 정확한 셈은 모델 토크나이저 종속. 목표치가 소프트하면 근사가 합리적
(정밀도 비용 대비 효과). "근사의 한계를 문서화하고 전환 조건을 정해두는" 의사결정 방식.

---

## 11. 회수 루프 설계 3결정: 주기 5분 · 회수본에도 포기 규칙 · 수동 XACK

**결정**: 방치 메시지 회수(XAUTOCLAIM)는 ① **5분 주기**의 별도 루프로 돌리고, ② 회수한 메시지에도
**포기 규칙(delivery count 임계)을 적용**하며, ③ 처리 성공 시 **직접 XACK** 한다. (`main.py reclaim_loop`)

**① 주기 5분 — 트레이드오프는 Redis 부하가 아니라 복구 지연**:
- 처음엔 "Redis 는 단일 스레드라 자주 돌리면 PEL 조회가 스레드를 잡는다"는 걱정으로 5분을 골랐으나,
  이 근거는 틀렸다 — XAUTOCLAIM 은 count 제한·커서가 있어 호출당 비용이 마이크로초 수준으로 고정이다.
- 실제 트레이드오프: **최악 복구 지연 = min_idle(5분) + 루프 주기**. 5분 주기면 최악 10분인데,
  PRD §9의 idle 허용치(5~10분) 안이라 채택. 주기를 줄이면 사는 건 복구 속도이지 Redis 부하 절약이 아니다.

**② 회수본에도 포기 규칙 — 독성 메시지의 무한 회수 고리를 끊는 유일 지점**:
```
회수 → 처리 → 실패 → ACK 없음 → PEL 잔류 → 5분 뒤 또 회수 → ...  (영원히)
```
- 처리할 때마다 워커를 죽이는 메시지는 위 궤도를 영원히 돈다. 회수 직후 delivery count 를 확인해
  임계(3) 도달 시 FAILED+ACK 해야 고리가 끊긴다.
- 사실 **포기 규칙이 주로 발동하는 곳이 회수 경로다** — 새 메시지는 거의 항상 count=1 이라 임계에
  안 걸리고, count 2·3 은 재전달(≈회수)로만 나온다. 회수에서 확인 안 하면 §4는 사실상 죽은 코드.
- XAUTOCLAIM 이 가져오는 순간 times_delivered 가 +1 되므로, 회수 직후 조회값이 "이번이 몇 번째"다.

**③ 수동 XACK — 자동 ACK 은 구독 핸들러 전용**:
- FastStream 의 "정상 return → 자동 ACK"은 `@broker.subscriber` 경로에서만 작동한다. 회수 루프는
  우리가 직접 만든 루프라 FastStream 이 존재조차 모른다 → 정상 처리 후 **직접 `XACK`**.
- 빼먹으면: 처리는 끝났는데 PEL 에 남아 **5분마다 영원히 재회수**된다(매번 "EMBEDDED → 스킵"으로
  싸게 끝나지만 낭비 + delivery count 만 계속 증가).
- 반대로 처리 실패(예외) 시엔 ACK 하지 않는다 — 다음 주기의 회수 대상으로 남긴다.

**구현 참고**: 새 메시지 구독에 `StreamSub(min_idle_time=...)`을 주면 '새 메시지 읽기'가 '방치 회수' 모드로
바뀌어 갓 온 메시지를 못 읽는다(실측). 그래서 **새 메시지 구독(FastStream) + 회수 루프(직접 구현)** 를
나란히 두는 구조가 됐다. 루프 자체는 어떤 예외에도 죽지 않게 감싼다 — 루프가 죽으면 크래시 복구 기능이
통째로 사라지므로.

💬 **면접 포인트**: "회수 주기를 어떻게 정하나" — 최악 복구 지연 = min_idle + 주기라는 식으로 답하기
(단일 스레드 걱정은 XAUTOCLAIM 복잡도를 물으면 무너진다). "독성 메시지를 어떻게 격리하나" — 재전달
카운트 기반 포기가 회수 경로에 있어야 하는 이유. "자동 ACK 의 경계" — 프레임워크 배관 밖에서는 부기가
전부 수동이 된다.

---

## 12. 내부 재시도: 외부 호출 3곳만, 지수 백오프 3회, 실패마다 retry_count 즉시 기록

**결정**: 일시 오류 내부 재시도(§9)는 **외부 호출 3곳**(S3 다운로드+추출 / LLM 구조화 / 임베딩)에만 건다.
실패마다 `retry_count` 를 DB 에 **즉시 +1** 하고(§6), 1s→2s→4s 지수 백오프 후 재시도, **최대 3회** 소진 시
예외를 전파한다(→ ACK 없음 → PEL → 회수 경로). (`pipeline._with_retry`)

- **왜 외부 호출만**: 일시 오류(네트워크·Bedrock throttle)가 나는 곳이 거기뿐이다. DB 호출은 재시도로 감싸지
  않는다 — DB 가 죽으면 retry_count 기록 자체가 안 되므로 감싸는 의미가 없고, 그 경우는 예외 전파 →
  재전달이 올바른 경로다.
- **왜 소진 시 전파**: 내부 재시도(초 단위, 같은 프로세스)와 재전달(분 단위, 회수 경로)은 **시간 스케일이
  다른 2단 방어**다. 짧은 장애는 내부 재시도가, 긴 장애는 PEL 재전달이 흡수하고, 그래도 안 되면
  delivery count 임계(§4)가 종결한다: 내부 3회 → 재전달 3회 → FAILED.
- **실패마다 즉시 DB 기록(§6)**: 재시도 도중 크래시해도 "몇 번 시도했는지"가 남는다(크래시 생존성).
  서버·운영자는 read-only 로 관측만.
- **리셋 규칙(§3.2)**: 신규 메시지 = 새 런 → 처리 시작 전에 0 리셋. 회수(XAUTOCLAIM) 재개 = 같은 런의
  연장 → 유지. `process_request(is_reclaimed=)` 인자로 구분한다.
- **MVP 단순화**: 예외를 일시/영구로 분류하지 않고 전부 재시도한다(3회 낭비 상한). 분류는 실제 Bedrock
  에러 유형을 본 뒤 정교화.

💬 **면접 포인트**: "재시도를 어디에 몇 번 거나" — 실패 원인의 시간 스케일별 2단 방어(초: 내부 백오프 /
분: 재전달)와 종결 장치(임계)의 합. "재시도 횟수를 왜 DB 에 기록하나" — 크래시 생존성과 관측성.

---

## 13. 청크 풍부화(enrichment): 필드 3종 · 이력서당 1회 · 필수 단계 + 실패 원인 기록

**결정**(2026-07-21): 청킹과 임베딩 사이에 **LLM 풍부화 단계**를 넣어 청크 metadata 에
`topics`·`relatedConcepts`·`questionHints` 를 병합한다. 같은 작업에서 **포기(§4) 시 error_message 에
마지막 실패 원인을 합류**시키는 개선도 함께.

**왜 이 3필드**: 사용자가 처음 그린 검색 응답 그림(questionHints 등)에서 출발. 기계적 꼬리표(type/label)로는
못 만들고 **LLM 이 내용을 읽어야** 나오는 것들만 추림. chunkType 세분화는 청킹 세분화(성과 단위)와 묶여서 보류.

**왜 이력서당 1회 호출**: 청크별 호출 대비 비용·지연 1/N + 청크 간 표기 일관(같은 주제를 다른 말로 부르는
흔들림 방지). 길이·순서 불일치는 오류 취급 → 재시도.

**왜 필수 단계**: "풍부화 있는/없는 청크"의 부분 상태를 만들지 않기 위해. 실패는 기존 복구 체계(내부 재시도
→ 전파 → 회수 → 포기)에 그대로 편입 — 새 상태·새 SSE 동작 없음.

**실패 원인 기록 (DB 상세 / SSE 간단 분리)**:
- 포기 규칙은 "수집형" 종결이라 원인을 모른다(판정형 FAILED 3종은 이미 원인 포함 — 전수 조사로 확인).
  내부 재시도 소진 시와 예상 밖 예외 시 **마지막 오류를 error_message 에 best-effort 기록**해 두고,
  포기 시점에 합류시킨다.
- **SSE 는 간단 문구 유지** — 예외 문구는 사용자 행동을 바꾸지 못하고 내부 정보 노출이므로.
- 한계: DB 자체가 죽으면 기록 불가(로그 담당). 기록 실패는 조용히 무시 — 부가 기능이 원래 예외 전파를
  가리면 안 된다.

💬 **면접 포인트**: "색인 시점 enrichment vs 조회 시점 생성" 트레이드오프(검색 필터 가능 vs 인덱싱 비용),
"부분 상태를 피하는 설계", "실패의 판정형/수집형 구분과 원인 추적", "내부 오류를 사용자에게 그대로
노출하지 않는 이유".

---

## 14. 청킹 세분화: 성과 단위 (실측 A/B 로 결정)

**결정**(2026-07-21): 프로젝트 청킹을 엔티티=청크에서 **성과 1개 = 청크 1개**로 세분화.
성과 경계 분리는 §13 풍부화 LLM 호출에 통합. 짧은 설명은 엔티티=청크 폴백. `chunk_version = 3`.

**계기 — 실측된 문제**: 실제 이력서에서 프로젝트 청크 하나에 성과 7개가 뭉침 → 임베딩 좌표가 평균으로
뭉개져 "DB 최적화" 질의의 1·2위 격차가 0.009 수준까지 좁아짐(정답은 맞지만 변별력 없음).

**A/B 실험**(실제 이력서 + 실제 임베딩, 질의 8개):

| 지표 | A(엔티티=청크) | B(성과=청크) |
|---|---|---|
| 정답 적중 | 7/8 | 7/8 |
| 평균 1·2위 격차 | 0.049 | **0.070 (+43%)** |
| 1위 유사도(예: 이메일 인증) | 0.341 | **0.440** |

- B 는 적중 시 **"어느 성과인지"까지 특정** — 질문 생성 재료의 질이 다름.
- 둘 다 틀린 질의("학교에서 공부") = 청킹 무관, 청크 내용 빈약 문제 → §13 topics 보조 검색으로 구제 가능
  (두 개선의 상호보완 확인).

**경계 분리를 LLM 에 맡긴 이유(사용자 선택 b)**: "어디까지가 소개고 어디부터가 성과인가"는 내용 이해가
필요한 판단 — 기계 규칙(앞 N문장)은 이력서마다 어긋난다. 이미 지불 중인 풍부화 호출(이력서당 1회)에
무임승차라 추가 비용 0. 대가: 청크 경계가 비결정적(실행마다 미세 차이) — REINDEX 백필로 복구 가능한
범위라 수용.

**계층형(F)을 안 만들고 부모 문맥을 얻는 법**: 성과 청크의 `source_index` 로 `structured_data` 의
프로젝트 원문을 조회하면 됨 — 부모가 이미 DB 에 있으므로 부모 청크 저장이 불필요.

💬 **면접 포인트**: "청킹 단위를 어떻게 정했나" — 감이 아니라 실측 A/B(격차 +43%)로 결정한 과정 자체.
"임베딩이 여러 주제를 담으면 왜 검색이 둔해지나"(좌표 평균화), "계층형 검색의 부모 문맥을 저장 없이
얻는 법"(원본 참조).

---

## 결정 요약 (색인)

| # | 결정 | 한 줄 근거 | 되돌리기 |
|---|---|---|---|
| 1 | 별도 레포 | 결합의 축은 코드가 아니라 계약 | 높음(합치기 어려움) |
| 2 | FastStream | I/O async + Streams 1급 | 중 |
| 3 | Redis Streams+Group | 유실 불가 작업 → PEL/재처리 | 중 |
| 4 | 계약 자기완결 사본 | 크로스 레포 리뷰 가시성 | 저(관리 방식) |
| 5 | 상태 기준 체크포인트 | at-least-once 멱등·재개 | 저 |
| 6 | DLQ 미도입 | 메시지가 포인터라 FAILED가 겸함 | 저 |
| 7 | Haiku + Cohere v3 | 형식준수 저가 / 한국어 검색 | **저**(Haiku↔Sonnet 무리스크 / 임베딩은 차원 바뀌면 스키마) |
| 8 | 진입 상태 Spring 세팅 | 소비자를 단순 상태기계로 | 중 |
| 9 | 청킹 C | 입력이 구조화 데이터 | **저**(REINDEX 백필) |
| 10 | 크기 측정 근사(글자÷2) | 512는 소프트 목표, 정밀 셈은 모델 종속 | **저**(REINDEX 백필) |
| 11 | 회수: 5분 주기·포기규칙·수동 XACK | 복구지연=idle+주기 / 독성 고리 절단 / 자동 ACK은 핸들러 전용 | 저(설정값) |
| 12 | 내부 재시도: 외부 호출만·백오프 3회 | 초(내부)/분(재전달) 2단 방어 + 임계 종결 | 저(설정값) |
| 13 | 풍부화 3필드·1회 호출·필수 + 원인 기록 | LLM만 만들 수 있는 질문 재료 / 부분 상태 회피 / DB상세·SSE간단 | 저(REINDEX 백필) |
| 14 | 성과 단위 청킹 (LLM 경계 분리) | 실측 A/B 격차 +43% / 경계는 내용 이해 필요 | 저(REINDEX 백필) |
