# 메시지 소비/발행 (Redis Stream consumer group)
faststream[redis]

# AWS: S3/MinIO 다운로드 + Bedrock(Cohere 임베딩)
boto3

# Claude(Bedrock) 구조화 호출
anthropic[bedrock]

# PDF 텍스트 추출
PyMuPDF

# PostgreSQL 직접 기록 + pgvector 타입
psycopg[binary]
pgvector

# 계약/설정 모델
pydantic
pydantic-settings
